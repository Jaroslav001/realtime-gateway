describe('WsOperatorJwtGuard', () => {
  describe('token validation (GW-02)', () => {
    it.todo('accepts tokens with type:operator claim');
    it.todo('rejects tokens without type:operator claim');
    it.todo('extracts operatorId and managedProfileIds from claims');
    it.todo('rejects expired tokens');
    it.todo('rejects missing tokens');
  });
});
